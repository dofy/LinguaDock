# LinguaDock Product Site

LinguaDock 的静态产品介绍站，使用 Vite 构建并通过 Cloudflare Workers Static Assets 部署。

```bash
pnpm install
pnpm dev
```

构建与本地预览：

```bash
pnpm build
pnpm preview
```

完成 Cloudflare 登录后部署：

```bash
pnpm deploy
```
